# v0.0.10 — 區網覆核與可攜版更新

- 區網 Web GUI：設定可手動啟停，預設 8080，連接埠可儲存，使用一次啟動有效的存取碼。
- 照片單击選取、雙擊放大；Enter 整組確認每張照片為同一車輛，後續仍可修改單張。
- 網頁共用資料庫，提供過期資料檢查及分頁。只限可信任 HTTP 區網，無自動防火牆設定。
- 分開檢查 GitHub commit 與 Release，拒絕版本降級。
- EXE 更新下載驗證後，經再次確認關閉主程式，備份／替換程式並重新啟動；失敗嘗試回復。
- 保留全部工作區資料、設定、自訂模型、OCR 與 CUDA 環境。
- 資料夾型 EXE，不是大型單一 EXE。v0.0.9 或更舊版首次升級需手動取得套件。

驗證：完整回歸 237 項通過；獨立內嵌 Chromium 以兩張合成照片驗證單擊、雙擊、Enter 與單張更正。真實 EXE 啟動、兩次離線安裝，以及隔離安裝目錄的自動替換／保留資料／重新啟動測試全部通過。真實跨裝置區網與不同防毒／防火牆環境仍須使用者端驗證。
