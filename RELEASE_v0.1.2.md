# v0.1.2 — 保留單張人工覆核判定

- 修正整組確認會覆蓋「不同車輛」標記的問題。
- 桌面及區網網頁的確認按鈕／Enter 只確認「尚未覆核」照片，保留不同車輛、不確定、已排除及既有註記。
- 按鈕改名為「確認剩餘未覆核」，確認後仍前往下一個待檢核群組，不另彈出確認視窗。
- 群組狀態依實際單張判定計算，不會把有不同車輛的群組誤標為全部一致。之後仍可修改單張判定。

完整回歸 248 項測試通過，涵蓋桌面按鈕、Return／Enter、網頁確認、重複確認及交易失敗復原。使用合成資料，不使用私人照片。

## 更新方式

在「專案資訊 → 線上檢查 Commit 與 Release」下載並確認套用；或下載 Windows x64 資料夾型 ZIP，保留完整資料夾，不能只搬移 EXE。

更新不覆寫照片、資料庫、設定、模型及 OCR／CUDA 環境。舊版先前已被覆蓋的單張標記無法自動還原，請手動重新確認。v0.1.1 的下載進度與背景校驗修正保留。
