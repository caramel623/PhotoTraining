# HANDOFF — 2026-09-10（ONNX 車牌偵測 + Phase 4 完成）

> 本檔記錄 2026-09-10 本次工作：把 Campus-Violation-Helper 的 ONNX 車牌
> 偵測模型搬進來、做成可切換的 plate engine，並完成 Phase 4（手動審核 GUI）。
> 說明用繁中，指令/程式碼用英文。前一次交接見 `HANDOFF.md`（Phase 2.5）。

---

## 1. Repo / 帳號現況

- **Repo**：`caramel623/PhotoTraining`（**私人**，default branch `main`）
  https://github.com/caramel623/PhotoTraining
- **帳號**：caramel623（gh 已登入，scopes: gist / read:org / repo / workflow）
- **本機**：git 已接 `origin`，身份 `caramel623 / 197044812+caramel623@users.noreply.github.com`
- **來源 repo**：`caramel623/Campus-Violation-Helper`（私人，車牌模型與授權檔取自此）

## 2. 已 commit + push 的 commit（`main`，全部上線）

| SHA | 內容 |
|---|---|
| `36d8455` | 7 支 MD 文件（README / PROJECT_README / HANDOFF / PHASE2 / PHASE3 / PHASE3_WRAPUP / PROGRESS）|
| `cdd84a0` | 原始碼（85 檔，總 366 KB）：`vehicle_dataset_manager/`、`tests/`、`scripts/`、`run.py`、`pyproject.toml`、`requirements*.txt`、`build_windows.ps1` |
| `060480b` | ONNX 車牌偵測器 + `models/MODEL_LICENSE.txt` + `onnxruntime` 相依 |
| `07b05b0` | 設定頁可切換 plate detector（none / onnx / paddle）|

### 排除大型二進位（`.gitignore` 已有）
`2023.7z`(1.5GB)、`2025.7z`(2.4GB)、`runs/`(5.1GB 照片+DB)、`yolov8n.pt`/`yolov8s.pt`、
`.venv/`、`.venv-ocr/`、`.cache/`、`Ultralytics/`、`*.db`、`*.pt`、`*.pth`、`models/*.onnx`。
> 規則：大型二進位/照片/模型權重**只留本機**，不進 git；授權文字完整保留。

## 3. 成果 A — ONNX 車牌偵測器（已上線）

取自 Campus-Violation-Helper 的 `models/taiwan_plate_detector.onnx` + `modules/plate_detector.py`
（YOLOv8 單類車牌區域偵測，640×640 letterbox，CPU，`onnxruntime`；訓練資料 TLPRR v7，CC BY 4.0）。

- **`vehicle_dataset_manager/detection/onnx_plate_detector.py`**（185 行）
  `OnnxPlateDetector(BaseDetector)`，`name="onnx-plate"`；`DEFAULT_MODEL_PATH` 指向
  `models/taiwan_plate_detector.onnx`；`detect()` 吃 BGR ndarray、回 `List[Detection]`。
  lazy import onnxruntime，`warmup()` 可預載。
- **`models/taiwan_plate_detector.onnx`**（11.67 MB）→ 本機，`models/*.onnx` 已 gitignore
- **`models/MODEL_LICENSE.txt`** → CC BY 4.0 歸屬文字**完整保留**並 commit
- **`requirements.txt`** 加 `onnxruntime>=1.17`（venv 已裝 1.29.0）

** Smoke test（已跑過）**：5 張 `runs/2025` 實際監視器照片全偵測到車牌，conf 0.92–0.94。

> 為什麼有用：原 plate 槽是 `PaddlePlateDetector`（全圖 OCR 猜車牌，docstring 寫
> "no separate plate model in v1"）。ONNX 偵測器先定位真正車牌框、再讓 PaddleOCR 讀
> crop，符合「plate 只是弱訊號、人工可覆核」原則。OCR 那層**不用換**（保留 PaddleOCR）。

## 4. 成果 B — 設定可切換 plate engine（已上線）

`Models` 群組 `Plate detector` 下拉可選 **none / onnx / paddle**，存檔後 `build_detectors()` 重建。

- **`core/config.py`**：`ModelsConfig.plate_detector`（docstring 更新為 none/onnx/paddle）
- **`app_context.py`**：
  - import `OnnxPlateDetector` + `DEFAULT_PLATE_MODEL_PATH`
  - `_build_ocr()`：當 `ocr==paddle` **或** `plate_detector==paddle` 都啟動**同一支** PaddleOCR sidecar
  - 新增 `_build_plate()`：`plate_detector==onnx` 時建 OnnxPlateDetector；**於 `_build_ocr()` 之後跑**，
    所以 onnx 選擇會覆蓋 paddle-plate；建錯（缺檔/沒裝 onnxruntime）保留先前的引擎當 fallback
- **`ui/settings_page.py`**：`model_plate` combo `["none","onnx","paddle"]` + `plate_status` 狀態列
  （`_update_plate_status()` 顯示 READY / MISSING / onnxruntime not installed）

**6 種 slot 組合（已驗證全過）**：
| plate | ocr | plate engine | ocr engine | sidecar |
|---|---|---|---|---|
| onnx | none | OnnxPlateDetector | StubOcr | 無 |
| none | none | Stub | Stub | 無 |
| paddle | none | PaddlePlateDetector | StubOcr | 有 |
| none | paddle | PaddlePlateDetector | PaddleOcr | 有 |
| onnx | paddle | OnnxPlateDetector | PaddleOcr | 有 |
| onnx 偵測 | - | 5 張實測 0.92–0.94 | - | - |

## 5. Phase 4 — 手動審核 GUI（**完成，尚未 commit**）

規格：README 十三（Review GUI）、十四（Merge/Split）、十五（人工標籤）。
目標：左側 Vehicle Group 清單、右上群組摘要（車牌/圖數/Camera/年份）、右中縮圖 Grid、
快捷鍵 `Enter`=同車、`Space`=下一張、`N`=非同車、`U`=不確定、`E`=編輯車牌、`M`=merge、`S`=split。

### Group A（domain 層，Qt-free）
- **`vehicle_dataset_manager/database/repositories.py`**（改）：
  新增 `VehicleRepository.members_for_review(vehicle_id)` — 一次 join 拿齊
  `vehicle_crop_path`/`review_status`/`date` 等，供 Review GUI 用（純讀、不改 DB）。
- **`vehicle_dataset_manager/review_model.py`**（新，171 行）— `ReviewModel`：
  - 讀：`list_groups(verification=)`、`group_summary()`、`group_images()`
    （`GroupSummary` / `GroupImage` dataclass；`display_path` 優先 vehicle crop、fallback 原圖）
  - 逐張：`set_image_status()`（走 `ReviewRepository.upsert`，會同步 `images.review_status`）、
    `edit_image_plate()`（改 plate 並把該圖 re-link 到新車牌群組；原始圖不改）
  - 群組：`suggest_verification()`（依成員 review 狀態推 VERIFIED/PARTIALLY_VERIFIED/AUTOMATIC_ONLY）、
    `confirm_group()`、`merge()`、`split()`（皆包裝既有 repos）
- **`tests/test_review_model.py`**（新，145 行，12 tests）

### Group B（GUI）
- `vehicle_dataset_manager/ui/review_page.py` 已由佔位頁替換為完整審核畫面：
  - 左側候選群組清單，右側摘要與縮圖 Grid。
  - 群組 verification 與逐圖 review status 篩選。
  - `unreviewed / same / not-same / uncertain / excluded` 狀態色碼與進度條。
  - 快捷鍵：`Enter` 同車、`Space` 下一張、`N` 非同車、`U` 不確定、
    `X` 排除、`E` 編輯車牌、`M` merge、`S` split。
  - 車牌編輯會正規化文字並重新連結候選群組；空值拒絕。
  - merge/split 均有確認對話框，只改 metadata 與 group relationship。
  - `QThreadPool` 背景載入縮圖，解碼時先縮小，避免大圖卡住 UI 或浪費 RAM。
- MainWindow 在切入 Review 分頁及處理完成後自動 refresh；關閉時會釋放 OCR sidecar。

### Group C（驗證）
- `tests/test_review_model.py`：**15 全綠**。
- `tests/test_review_page.py`：**4 全綠**（offscreen；含狀態、快捷鍵、改牌、merge/split）。
- Phase 4 專屬：**19 全綠**；全專案：**111 全綠**。
- `scripts/smoke_ui.py`：`UI SMOKE OK`，以現有 `runs/2025` DB 啟動通過。
- 本次沒有執行新照片分析批次（真實照片處理數 **0**，符合最多 100 張限制）。

## 6. 已知問題 / 注意

- 沙箱跑 pytest 時 `%TEMP` 會 PermissionError → 用 `$env:TMP=$env:TEMP=<專案>/.tmp`
  迴避；`.tmp` 是測試暫存目錄，不納入產品資料。
- `review_model.py` 原本被 PowerShell `Set-Content` 加了 BOM 會 import 失敗，**已修**（改寫成 no-BOM）。
- Phase 4 已以 commit `996c3c1` 推送到 `caramel623/PhotoTraining` 的 `main`。

## 7. Phase 5 完成（Dataset Export）

1. 完成 vehicle crop、plate crop、車牌遮罩 Re-ID crop；遮罩支援純色、模糊與 inpaint。
2. 安全預設只採 `human_verified`，可要求可靠 plate bbox；原圖只複製、永不修改。
3. 完成 group hash／時間／攝影機切分，同車不會跨 train/val/test。
4. 完成正負 pair 與 triplet；優先跨攝影機正樣本及困難負樣本。
5. 產出 portable manifest/CSV/config/state 與 DB 匯出紀錄，不洩漏來源絕對路徑。
6. 可取消與同設定續跑；已完成輸出會依來源更新時間檢查後重用，設定不同會拒絕覆寫。
7. Export GUI 已接上背景工作、預覽、取消、續跑及進度顯示。
8. Phase 5 專項 **21 全綠**；全專案 **132 全綠**；`UI SMOKE OK`。
9. P5 驗證全用合成影像；真實照片處理／匯出 **0 張**。

詳見 `PHASE5.md`。下一步為 Phase 6：Re-ID embedding engine 與 AI02 交付。

## 9. 2026-09-11 Phase 6 本機整合完成

- 新增可替換 `OnnxReIDEngine`：明確 RGB/NCHW/normalization，輸出強制 L2 normalize。
- 新增不依賴 FAISS 的 portable cosine index（NPZ，`allow_pickle=False`）。
- P5 manifest 可批次建立 embedding，支援 checkpoint、取消與續跑。
- Dataset 或模型 identity 改變時拒絕沿用舊索引，避免 embedding 混用。
- 索引 metadata 不含車牌文字，僅保留車輛、相機、時間、split 與 crop 路徑。
- Settings／AppContext／Dataset Export GUI 已完成 ONNX model slot 與 Build Re-ID Index。
- 新增 `scripts/build_reid_index.py`，Windows 與 AI02 共用。
- 新增 `ai02/` Docker、ONNX Runtime GPU 與 FAISS converter 交接；不啟用 REST API 或自動同步。
- `.dockerignore` 採 allowlist，Docker build context 不含照片、runs、DB、模型與其他私人資料。
- P6 核心／wiring 測試 14 項，相關 Export GUI 測試 3 項；全專案 **146 全綠**，`UI SMOKE OK`。
- 本階段使用合成影像；真實照片處理／embedding **0 張**，沒有下載模型。
- AI02 RTX A5000、CUDA provider、正式 ONNX 模型與 FAISS GPU 尚待實機驗證。

### 關鍵資料模型（供 resume）
- `images.review_status`：`unreviewed / verified_same_vehicle / verified_not_same_vehicle / uncertain / excluded`
- `vehicles.verification`：`automatic_only / partially_verified / verified`
  （label_priority：human_verified > high_confidence_plate_match > automatic_candidate）
- `VehicleRepository`：`list_vehicles` / `members_for_review` / `add_member` / `remove_member` /
  `get_or_create_for_plate(plate, source=)` / `merge(src,tgt)->tgt` / `split(vid, ids)->new_id` /
  `set_verification` / `get`
- `ReviewRepository.upsert(image_id, status, vehicle_id=, note=)` — 同時更新 `images.review_status`
- 圖檔路徑：`images.source_path`（原圖）、`images.vehicle_crop_path`（vehicle crop），皆完整磁碟路徑
- `Workspace`：`thumbs_dir`（`<ws>/thumbs`）可放縮圖快取

## 8. 工具 / 環境注意（resume 時必看）

- **`exec_command`**：PowerShell 下需要 `login=true`；有時要 `sandbox_permissions=require_escalated`
  才會通過。**並行呼叫（一次兩個 tool block）會被 policy 拒絕** → 一次一個。
- **PowerShell 寫檔會加 BOM**（`Set-Content -Encoding utf8`）→ Python 檔寫完用
  `[IO.File]::WriteAllText(path, text, UTF8Encoding($false))` 去 BOM。
- **PowerShell 吃引號/`&&`**：多行 Python 用 `@'...'@` heredoc 存成 temp .py 再跑；
  別用 `&&`（用 `;`）；SQL/`-c` 內嵌引號易翻車 → 改寫 temp script。
- **下私有 repo 原始檔**：`gh api .../contents/<path> -H "Accept: application/octet-stream"` 會 415；
  改用 `Invoke-WebRequest` 加 `Authorization: Bearer <gh token>` 打 `download_url`。
- 環境：Python 3.14 主 venv（`.venv`）；`.venv-ocr` Python 3.13（paddle 3.3.1 + paddleocr 3.7.0）；
  torch 2.14.0+cpu；PySide6 6.11；本機 AMD（無 CUDA）→ CPU 開發。
- **用家偏好**（HANDOFF.md §10）：繁中說明；分組建＋逐組驗證；每 Phase 可獨立跑測；
  不確定 API/套件先查本機已裝版本＋官方文件、別猜；優先序：穩定 > 不丟資料 > 可 resume >
  易人工標註 > 本地安全 > 大圖效能 > 可換模型 > UI 美觀（最後）；全本地、原始圖不改。

## 10. 2026-09-11 v0.0.2 繁體中文介面

- 主視窗、八個頁籤、按鈕、表頭、篩選器、狀態列、提示與錯誤對話框均改為繁體中文。
- 工作、複核、群組來源與確認狀態會顯示中文名稱，但資料庫與設定檔的內部代碼維持不變。
- 遮罩方式、模型開關及 PaddleOCR 預設使用「中文顯示文字＋原始 data 值」，不影響舊設定。
- 載入 Qt「qtbase_zh_TW」，讓「是／否」等系統按鈕也使用繁體中文。
- 版本升至 0.0.2；Windows 發行仍為 onedir 資料夾版，不製作大型單一 EXE。
- 完整測試全數通過；正式 EXE 已驗證中文視窗標題、全新工作區與資料庫建立。
- 本次沒有讀取或處理真實照片（**0 張**），模型、照片、資料庫與工作區不會加入發行檔。
- computer-use 畫面擷取因 Windows 沙箱 helper 錯誤無法啟動；已改以 Qt 元件測試、
  Qt 翻譯載入測試及實際 EXE 視窗標題完成驗證。

## 11. 2026-09-11 v0.0.3 設定頁捲動修正

- 設定群組改放在可垂直捲動的內容區，小視窗不再把所有欄位擠在一起。
- 「儲存設定」按鈕固定在捲動區外的底部，任何捲動位置都能直接操作。
- 新增 640×420 小視窗回歸測試，驗證捲動範圍存在且儲存按鈕不在捲動內容內。
- 未指定工作區時，預設使用 VehicleDatasetManager.exe 所在資料夾；命令列或設定指定的路徑仍優先。
- 本次沒有讀取或處理真實照片（**0 張**）。
