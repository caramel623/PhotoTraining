# 區網覆核與更新功能續作

## 已實作（v0.0.10；2026-09-15 完成建置驗證）

- 桌面單擊選取、雙擊放大。
- Enter 整組確認會在單一交易中把所有成員設為同一車輛；後續單張更正仍可進行。
- 設定新增區網覆核啟停及連接埠，預設 8080，可儲存。
- 繁體中文網頁：群組／照片分頁、存取碼、單擊／雙擊、整組確認、單張狀態與車牌修改。
- HTTP 執行緒不直接操作 Qt 或資料庫；請求透過有界佇列於主執行緒執行。限制八個同時 HTTP 執行緒。
- 檢查群組內容版本避免過期修改；停止後舊存取碼／排隊操作失效；只供應工作區內影像。
- 啟停不變更 Windows 防火牆；服務使用 HTTP，僅適用可信任區網。
- GitHub commit／Release 檢查、SHA256 與清單驗證、確認關閉後更新輔助流程已實作；僅替換 EXE／_internal／清單，備份且失敗回復。

## 驗證

- 最終完整測試：237 passed in 54.22s。較早一輪曾有 frozen pip subprocess 編碼警告，最終一般執行模式未出現。
- 隨後補強交易回復及服務失效保護，覆核／設定相關 36 項測試全部通過。
- .tmp/check_v008_ui.py 空工作區 MainWindow 冒煙測試通過。
- 使用合成影像；沒有處理或上傳真實照片。
- 獨立內嵌 Chromium 合成測試已驗證登入、圖片載入、單擊、雙擊、Enter、單張修改；尚未做真實跨裝置區網驗證。
- 更新器 12 項合成測試通過，包含 rollback、保留私人資料、ZIP 越界、校驗及不降級。

## EXE 與發布核對

1. 2026-09-15 確認 GitHub repo 為 PUBLIC。六個舊 Release v0.0.1／2／3／4／6／7 全部下載，SHA256 與 GitHub digest 一致、ZIP 完整性通過；圖片逐位元組符合公開依賴資源，模型符合授權 SHA256，未找到私人照片、DB 或憑證候選。稽核保留在 .tmp/release-public-audit-20260914/audit.json，不上傳測試資料。
2. 真實 v0.0.10 EXE：空工作區啟動及兩次離線 pip 安裝通過。
3. 真實凍結更新輔助程式端到端通過：等待測試父程序退出、備份舊程式、替換、保留合成私人資料、重新啟動至原指定工作區。測試僅操作 .tmp/frozen-update-n9il8wd_，沒有更新使用者實際安裝。
4. 功能 commit：a02650b。使用者已明確授權推送程式與完整教學，並發布 v0.0.10；上傳前需打包 ZIP、校驗並確認工作樹乾淨。
5. 套件為 dist/VehicleDatasetManager 資料夾型 EXE。自動更新只替換 EXE、_internal、update-manifest.json；照片、DB、模型、設定、OCR／CUDA 環境保留。
6. 仍未驗證真實跨裝置區網、使用者端防火牆及不同防毒軟體環境；HTTP 僅限可信任區網。
