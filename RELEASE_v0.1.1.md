# v0.1.1 — 自動更新進度與介面回應修正

- 下載時顯示已下載／總 MB、百分比與平均下載速度。
- 解壓縮及逐檔校驗顯示處理檔案數；處理期間提供活動進度條與經過秒數。
- 同一階段 15 秒沒有新進度時提示可能正在等待網路或磁碟，不把等待誤判成成功或失敗。
- 套用前的完整校驗改在背景執行，避免介面無回應；等待更新輔助程式時禁止重複操作。
- 保留 SHA-256、路徑檢查、備份及失敗復原機制，不覆寫照片、資料庫、設定、模型及 OCR／CUDA 環境。

完整回歸測試 241 項通過，使用合成資料，不使用私人照片。

## 如何更新

在「專案資訊 → 線上檢查 Commit 與 Release」下載新版，校驗後確認關閉並套用。也可以下載 Windows x64 資料夾型 ZIP；必須保留完整資料夾，不能只搬移 EXE。

舊版升級到 v0.1.1 的當次下載仍使用舊版更新介面；新的進度顯示於啟動 v0.1.1 後的更新流程生效。主程式關閉後的檔案替換仍由背景輔助程式執行，完成後自動重啟，請勿手動啟動第二個程式。
