# v0.0.8 原圖替換與本機 EXE 交付

## 完成內容

- 接續 2026-09-12 的 Qt 日誌崩潰修復、照片完整性掃描／修復、備份清除資料庫。
- 新增 OriginalReplacementService 兩階段原圖替換，ZIP／7Z 或原圖資料夾皆可。
- 以完整檔名（不分大小寫）唯一配對；來源或 DB 同名多筆、無配對、不可解碼均不替換。不新增影像。
- 未處理且無人工操作直接替換；已處理／失敗／略過或人工標註／群組者先確認。介面是／否／取消分別代表全換、只換未處理、全不換。
- 保留 image_id、人工車牌、INI、review_status、reviews 及 vehicle_members。新照片存 extracted/originals-*；舊原照片保留。
- 清除舊 bbox、crop 引用、OCR 及 duplicates，標記 pending；使用者需按「影像處理」重建。重建時不再把既有群組改為新 OCR 候選。
- 替換前 JSON checkpoint 與 replacement-report.json 放在新快照。無一鍵撤回；已匯出資料集不自動更新。
- 預覽至套用間重新核對資料和副本雜湊，避免覆蓋後來修改的人工資料。逐筆交易，可取消；保留已成功項目。
- 版本 0.0.8；本輪只編譯本機 EXE，不 commit、不推送、不建立 GitHub Release。

## 驗證

- 完整回歸：**211 passed in 46.88s**。
- 新增原圖替換 6 項核心測試與 4 項確認介面測試；全部使用合成影像，真實照片 0 張。
- 完整主視窗來源模式 smoke 與中文布局截圖通過（新空白工作區）。
- build_windows.ps1 onedir 成功（2026-09-12 晚間完成，因額度限制於 09-13 接續核對）。
- 正式 EXE 啟動空白工作區維持 10 秒，app.log 記錄 v0.0.8，DB 影像數 0；測試程序由測試腳本結束，不影響使用者程序。
- 正式 EXE --check-installer：frozen=true，兩次 success／installed／launcher_created=true，returncode=0。
- 發行目錄未發現 JPG／JPEG／INI／DB／SQLite／日誌／settings.json。PNG 僅 matplotlib 工具列圖示。
- 打包車牌權重 SHA256：E40B1ABEC9818430D9AA1EA522A65DEE79AE98A502F1B548CFC831B70FD16BE4。

## 交付與限制

- ZIP：dist/VehicleDatasetManager-v0.0.8-windows-x64.zip，373,997,685 bytes，4,229 個檔案；CRC 與逐檔清單核對通過。
- SHA256：3be08676448e2a73a3d350e2edb109c6a74e46254dc92d71fc89b8f8bafb0776（另有 .zip.sha256）。

- EXE：dist/VehicleDatasetManager/VehicleDatasetManager.exe。必須保留整個資料夾，不能只搬 EXE。
- 完整教學：README.md。舊的崩潰交接文件描述的是本輪編譯前狀態，以本文件為準。
- 未在使用者 A5000 上做原生崩潰重現或實際 GPU 推論，沒有 native crash dump，不保證此修正涵蓋所有閃退原因。
- 不修改使用者 E: 工作區，不清除現有資料庫，不執行真實照片替換。CUDA 仍另行下載。
