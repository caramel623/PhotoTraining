# Vehicle Dataset Manager v0.0.6

## 本次更新

- INI 結構化解析與原始資料／多來源追蹤。
- 區分壓縮檔已匯入與解析器版本；相同壓縮檔也可補掃新版 metadata。
- 影像 SHA256 跨壓縮檔改名、重新壓縮與 ZIP／7Z 去重。
- 人工車牌優先，保護人工群組合併／拆分、確認、排除與註記。
- 取消續跑、逐影像交易、匯入歷史及未分組影像衝突佇列。
- 包含 v0.0.5 的必要依賴／OCR 模型安裝功能及指定台灣車牌 ONNX 權重。
- GitHub README 已改成完整繁體中文安裝、重掃、複核、匯出與備份教學。

## 下載與更新

下載 Windows x64 ZIP 後完整解壓，執行 VehicleDatasetManager.exe。這是資料夾式發行，不要只複製 EXE。

更新前請關閉程式並備份工作區，保留原本的 database、extracted、crops、settings.json 等資料。資料庫會自動遷移，不要刪除 DB 重建。

舊資料補 INI：匯入頁加入原壓縮檔 → 選「重新掃描既有資料」→ 在清單選取檔案 → 開始重掃。此模式不新增影像，也不重跑 AI。

## 驗證與限制

183 項自動測試通過；合成資料 UI 檢查及編譯版空白工作區啟動測試通過。使用真實照片 0 張，發行包未加入私人照片、INI、資料庫或設定。

大型 7Z 取消可能需要等待當前解壓結束；缺少原始本地影像的舊記錄無法安全補算 SHA256；多來源衝突保留待人工處理。AI02 A5000 實機 CUDA／正式 Re-ID 模型仍需在目標機驗證。

ZIP SHA256：`83569BB400798C730C95A8B1637F5D88623322D455E475AD8E5EBA2B37243927`
